# PowerFS FUSE 客户端配置 (容器内)
# 精简配置：只需 [global] + [fuse] 段
# filer_addresses 和 volume_addresses 省略，从 master 拓扑动态发现

[global]
log_level = "info"
redis_url = "redis://172.30.0.50:6379"

[fuse]
mount_point = "/mnt/fuse"
master_addresses = ["172.30.0.11", "172.30.0.12", "172.30.0.13"]
master_net_port = 9334
volume_net_port = 8901
filer_net_port = 9334
collection = "default"
replication = "000"
threads = 8
verbose = true
container = true
log_file = "/var/log/powerfs-fuse.log"
request_timeout_secs = 10            # 测试环境: 10s, 给 Raft 共识足够时间
admin_port = 9999                    # Admin/debug HTTP server for request stats

[fuse.lease]
mode = "range"
lease_duration_ms = 30000
renew_interval_ms = 10000
